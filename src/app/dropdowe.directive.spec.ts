import { DropdoweDirective } from './dropdowe.directive';

describe('DropdoweDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdoweDirective();
    expect(directive).toBeTruthy();
  });
});
